---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
---

<BaseLayout title="Community Blog - DiscoLink Use Cases">
  <Header />

  <main>
    <div class="container">
      <nav class="breadcrumb">
        <a href="/use-cases">Use Cases</a> / <span>Community Blog</span>
      </nav>

      <header class="page-header">
        <span class="icon">✍️</span>
        <h1>Community Blog</h1>
        <p>Transform Discord discussions into blog posts. Share community content with the wider web and boost your SEO.</p>
      </header>

      <section class="overview">
        <h2>How It Works</h2>
        <div class="steps">
          <div class="step">
            <span class="step-num">1</span>
            <h3>Community shares in Discord</h3>
            <p>Members post tutorials, guides, insights, or stories in your forum channels.</p>
          </div>
          <div class="step">
            <span class="step-num">2</span>
            <h3>Tag quality content</h3>
            <p>Use a "featured" or "blog" tag to mark threads that should appear on the public blog.</p>
          </div>
          <div class="step">
            <span class="step-num">3</span>
            <h3>Blog auto-publishes</h3>
            <p>Tagged threads become blog posts with author attribution, timestamps, and social sharing.</p>
          </div>
        </div>
      </section>

      <section class="benefits">
        <h2>Benefits</h2>
        <div class="benefits-grid">
          <div class="benefit">
            <h3>Community Content</h3>
            <p>Blog content comes from your community, not just your team. Authentic voices and perspectives.</p>
          </div>
          <div class="benefit">
            <h3>Author Attribution</h3>
            <p>Each post shows the Discord author. Community members get credit for their contributions.</p>
          </div>
          <div class="benefit">
            <h3>SEO Benefits</h3>
            <p>Unique, regularly updated content that ranks in search engines. Drive organic traffic.</p>
          </div>
          <div class="benefit">
            <h3>Social Sharing</h3>
            <p>OpenGraph and Twitter cards for beautiful social previews when posts are shared.</p>
          </div>
        </div>
      </section>

      <section class="implementation">
        <h2>Implementation Options</h2>
        <div class="options-grid">
          <div class="option">
            <h3>Blog Template</h3>
            <p>Pre-built blog template with post cards, author bios, and table of contents.</p>
            <pre><code>npx create-discolink@latest --template blog</code></pre>
            <a href="/templates" class="link">View Templates →</a>
          </div>
          <div class="option">
            <h3>Filter by Tag</h3>
            <p>Use the API to fetch only threads with your "blog" or "featured" tag.</p>
            <pre><code>GET /servers/:id/threads?tags=blog,featured</code></pre>
            <a href="https://docs.discolink.site/api/overview" target="_blank" rel="noopener" class="link">API Reference →</a>
          </div>
          <div class="option">
            <h3>Static Export</h3>
            <p>Export blog posts as static HTML with full SEO optimization.</p>
            <pre><code>discolink export --server YOUR_ID --template blog --output ./blog</code></pre>
            <a href="https://docs.discolink.site/guides/cli-reference" target="_blank" rel="noopener" class="link">CLI Documentation →</a>
          </div>
        </div>
      </section>

      <section class="cta">
        <h2>Ready to build your community blog?</h2>
        <a href="/quickstart" class="btn btn-primary">Get Started</a>
      </section>
    </div>
  </main>

  <Footer />
</BaseLayout>

<style>
  main {
    padding-top: 120px;
    padding-bottom: 4rem;
  }
  .container {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 2rem;
  }
  .breadcrumb {
    margin-bottom: 2rem;
    font-size: 0.9rem;
    color: var(--text-secondary);
  }
  .breadcrumb a {
    color: var(--text-secondary);
  }
  .breadcrumb a:hover {
    color: var(--accent);
  }
  .page-header {
    text-align: center;
    margin-bottom: 4rem;
  }
  .icon {
    font-size: 4rem;
    display: block;
    margin-bottom: 1rem;
  }
  .page-header h1 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }
  .page-header p {
    color: var(--text-secondary);
    font-size: 1.15rem;
    max-width: 600px;
    margin: 0 auto;
  }

  section {
    margin-bottom: 4rem;
  }
  h2 {
    font-size: 1.75rem;
    margin-bottom: 1.5rem;
  }

  .steps {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  .step {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
  }
  .step-num {
    width: 40px;
    height: 40px;
    background: var(--accent);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    flex-shrink: 0;
  }
  .step h3 {
    font-size: 1.1rem;
    margin-bottom: 0.25rem;
  }
  .step p {
    color: var(--text-secondary);
    font-size: 0.95rem;
  }

  .benefits-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }
  .benefit {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1.5rem;
  }
  .benefit h3 {
    font-size: 1.1rem;
    margin-bottom: 0.5rem;
  }
  .benefit p {
    color: var(--text-secondary);
    font-size: 0.95rem;
  }

  .options-grid {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  .option {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 1.5rem;
  }
  .option h3 {
    font-size: 1.1rem;
    margin-bottom: 0.5rem;
  }
  .option p {
    color: var(--text-secondary);
    margin-bottom: 1rem;
  }
  .option pre {
    background: var(--bg-secondary);
    padding: 1rem;
    border-radius: 8px;
    overflow-x: auto;
    margin-bottom: 1rem;
  }
  .option code {
    font-size: 0.9rem;
    color: var(--text);
  }
  .link {
    color: var(--accent);
    font-weight: 500;
  }

  .cta {
    text-align: center;
    background: var(--bg-secondary);
    border-radius: 16px;
    padding: 3rem;
  }
  .cta h2 {
    margin-bottom: 1.5rem;
  }
  .btn-primary {
    display: inline-block;
    background: var(--accent);
    color: white;
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    font-weight: 500;
  }
  .btn-primary:hover {
    text-decoration: none;
  }

  @media (max-width: 768px) {
    .benefits-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
