---
const links = {
  product: [
    { href: '/quickstart', label: 'Quickstart' },
    { href: '/features', label: 'Features' },
    { href: '/use-cases', label: 'Use Cases' },
    { href: '/templates', label: 'Templates' },
  ],
  resources: [
    { href: '/docs', label: 'Documentation' },
    { href: '/docs/api', label: 'API Reference' },
    { href: '/docs/guides', label: 'Guides' },
    { href: '/docs/cli', label: 'CLI Reference' },
  ],
  links: [
    { href: 'https://github.com/KevinTrinh1227/discordlink', label: 'GitHub' },
    { href: 'https://github.com/KevinTrinh1227/discordlink/issues', label: 'Report Issue' },
  ],
};
---

<footer class="footer">
  <div class="container">
    <div class="footer-grid">
      <div class="footer-brand">
        <span class="logo">DiscordLink</span>
        <p>Open-source platform for syncing Discord content. Build knowledge bases, integrate with your apps via REST API, or export static sites. Use it however you want.</p>
      </div>

      <div class="footer-links">
        <div class="link-group">
          <h4>Product</h4>
          {links.product.map((link) => (
            <a href={link.href}>{link.label}</a>
          ))}
        </div>
        <div class="link-group">
          <h4>Resources</h4>
          {links.resources.map((link) => (
            <a href={link.href}>{link.label}</a>
          ))}
        </div>
        <div class="link-group">
          <h4>Links</h4>
          {links.links.map((link) => (
            <a href={link.href} target="_blank" rel="noopener">{link.label}</a>
          ))}
        </div>
      </div>
    </div>

    <div class="footer-bottom">
      <p>&copy; {new Date().getFullYear()} DiscordLink. MIT License.</p>
      <p class="disclaimer">DiscordLink is not affiliated with, endorsed by, or connected to Discord Inc.</p>
    </div>
  </div>
</footer>

<style>
  .footer {
    background: var(--bg-secondary);
    border-top: 1px solid var(--border);
    padding: 4rem 0 2rem;
    margin-top: 6rem;
  }
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }
  .footer-grid {
    display: grid;
    grid-template-columns: 2fr 3fr;
    gap: 4rem;
  }
  .footer-brand {
    max-width: 300px;
  }
  .logo {
    font-weight: 700;
    font-size: 1.25rem;
    display: block;
    margin-bottom: 1rem;
  }
  .footer-brand p {
    color: var(--text-secondary);
    font-size: 0.95rem;
  }
  .footer-links {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }
  .link-group h4 {
    font-size: 0.9rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-secondary);
    margin-bottom: 1rem;
  }
  .link-group a {
    display: block;
    color: var(--text);
    font-size: 0.95rem;
    padding: 0.3rem 0;
  }
  .link-group a:hover {
    color: var(--accent);
  }
  .footer-bottom {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid var(--border);
    text-align: center;
    color: var(--text-secondary);
    font-size: 0.9rem;
  }
  .disclaimer {
    margin-top: 0.5rem;
    font-size: 0.8rem;
    opacity: 0.7;
  }
  @media (max-width: 768px) {
    .footer-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .footer-links {
      grid-template-columns: 1fr;
    }
  }
</style>
